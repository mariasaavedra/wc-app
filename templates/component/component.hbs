import { MaybeParentComponentProps } from "../interfaces/ParentComponentProps";
import styles from '../styles/components/{{name}}.module.scss';

export interface {{name}}Props extends MaybeParentComponentProps {
}

export default function {{name}}(props: {{name}}Props) {
    return (
        <div className={styles.{{name}}Component}>
        </div>
    );
}